# 统一引用显示组件集成说明

## 完成情况

### ✅ 已完成
1. **后端RAG metadata修复** - OpenAI兼容模式现在正确发送document metadata
2. **创建统一引用显示组件** - `unified_reference_display.dart`
3. **添加翻译键** - zh-CN.json 和 en-US.json
4. **生成locale keys** - 运行了 easy_localization:generate

### 📝 待完成
1. 更新 `unified_reference_display.dart` 使用正确的locale keys
2. 修改 `ai_text_message.dart` 使用新的统一组件替换现有的三个组件
3. 测试并验证UI效果

## 详细步骤

### 步骤1：更新统一引用组件使用locale keys

在 `unified_reference_display.dart` 第114-127行，将硬编码的字符串替换为locale keys：

```dart
// 修改前
title = '网络搜索引用'; // 暂时使用硬编码
title = '文档引用';
title = 'MCP 工具调用';

// 修改后
import 'package:appflowy/generated/locale_keys.g.dart';
import 'package:easy_localization/easy_localization.dart';

switch (widget.referenceType) {
  case ReferenceType.webSearch:
    icon = Icons.language;
    title = LocaleKeys.chat_webSearchReferences.tr();
    break;
  case ReferenceType.document:
    icon = Icons.description_outlined;
    title = LocaleKeys.chat_documentReferences.tr();
    break;
  case ReferenceType.mcpTool:
    icon = Icons.extension_outlined;
    title = LocaleKeys.chat_mcpToolReferences.tr();
    break;
}
```

### 步骤2：更新 ai_text_message.dart

在 `ai_text_message.dart` 中（约第340-370行），替换现有的三个独立组件：

```dart
// 修改前（分别使用三个组件）
// 网络搜索结果引用显示
if (_hasWebSearchCitations(state.sources))
  Padding(
    padding: const EdgeInsetsDirectional.only(start: 4.0, top: 8.0),
    child: CitationDisplay(
      citations: _extractWebSearchCitations(state.sources),
      maxVisibleCitations: 3,
      showExpandButton: true,
    ),
  ),

// MCP工具引用显示
if (_hasMCPReferences(state.sources))
  Padding(
    padding: const EdgeInsetsDirectional.only(start: 4.0, top: 8.0),
    child: MCPReferenceDisplay(
      references: _extractMCPReferences(state.sources),
      showExpandButton: true,
      onReferenceSelected: onSelectedMetadata,
    ),
  ),

// 文档来源元数据显示
if (_hasDocumentSources(state.sources))
  SelectionContainer.disabled(
    child: AIMessageMetadata(
      sources: _extractDocumentSources(state.sources),
      onSelectedMetadata: onSelectedMetadata,
    ),
  ),

// 修改后（使用统一组件）
import 'package:appflowy/plugins/ai_chat/widgets/unified_reference_display.dart';

// 网络搜索引用
if (_hasWebSearchCitations(state.sources))
  Padding(
    padding: const EdgeInsetsDirectional.only(start: 4.0, top: 8.0),
    child: UnifiedReferenceDisplay(
      references: state.sources
          .where((source) => source.source == 'web' && isURL(source.id))
          .toList(),
      referenceType: ReferenceType.webSearch,
      maxVisibleReferences: 3,
      showExpandButton: true,
      onReferenceSelected: onSelectedMetadata,
    ),
  ),

// MCP工具引用
if (_hasMCPReferences(state.sources))
  Padding(
    padding: const EdgeInsetsDirectional.only(start: 4.0, top: 8.0),
    child: UnifiedReferenceDisplay(
      references: _extractMCPReferences(state.sources),
      referenceType: ReferenceType.mcpTool,
      maxVisibleReferences: 3,
      showExpandButton: true,
      onReferenceSelected: onSelectedMetadata,
    ),
  ),

// 文档引用
if (_hasDocumentSources(state.sources))
  Padding(
    padding: const EdgeInsetsDirectional.only(start: 4.0, top: 8.0),
    child: UnifiedReferenceDisplay(
      references: _extractDocumentSources(state.sources),
      referenceType: ReferenceType.document,
      maxVisibleReferences: 3,
      showExpandButton: true,
      onReferenceSelected: onSelectedMetadata,
    ),
  ),
```

### 步骤3：测试步骤

1. **重新编译Rust后端**：
   ```bash
   cd rust-lib
   cargo build
   ```

2. **运行Flutter应用**：
   ```bash
   cd appflowy_flutter
   flutter run
   ```

3. **测试场景**：
   - 测试网络搜索引用显示
   - 测试选择文档后的引用显示
   - 测试MCP工具调用的引用显示（如果有）
   - 验证所有引用的风格是否一致

## UI效果预期

所有引用现在应该具有一致的外观：

```
┌─────────────────────────────────────────┐
│ 🌐 网络搜索引用            3 个         │
├─────────────────────────────────────────┤
│ ① 🌍 标题文本（蓝色）                    │
│    域名（灰色小字）                      │
├─────────────────────────────────────────┤
│ ② 🌍 标题文本                            │
│    域名                                  │
├─────────────────────────────────────────┤
│ ③ 🌍 标题文本                            │
│    域名                                  │
│ 查看全部 5 个 ▼                         │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 📄 文档引用                2 个文档      │
├─────────────────────────────────────────┤
│ ① 📄 文档名称（蓝色）                    │
│    AppFlowy 文档（灰色小字）             │
├─────────────────────────────────────────┤
│ ② 📄 另一个文档                          │
│    AppFlowy 文档                         │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 🧩 MCP 工具调用           1 个工具       │
├─────────────────────────────────────────┤
│ ① 🧩 server.tool_name（蓝色）✓           │
│    MCP 工具（灰色小字）                  │
└─────────────────────────────────────────┘
```

## 关键特性

1. **统一的圆形序号**：所有引用都使用相同样式的序号
2. **一致的布局**：标题、副标题、元信息的布局完全一致
3. **统一的交互**：展开/折叠功能相同
4. **统一的配色**：使用theme主题色，支持深色/浅色模式
5. **可点击**：所有引用都可以点击（网络搜索打开链接，文档打开文档页）

## 注意事项

1. **编译顺序**：必须先编译Rust后端，然后再运行Flutter前端
2. **翻译文件**：如果需要支持更多语言，需要在其他语言的JSON文件中添加相同的keys
3. **向后兼容**：旧的组件（CitationDisplay、MCPReferenceDisplay、AIMessageMetadata）仍然存在，不影响现有代码
4. **逐步迁移**：可以先在一个地方测试UnifiedReferenceDisplay，确认效果后再全面替换

## 下一步优化建议

1. **添加动画**：引用项出现时使用淡入动画
2. **加载状态**：文档引用加载真实名称时显示loading状态
3. **错误处理**：网络搜索打开失败时显示提示
4. **可访问性**：添加screen reader支持
5. **性能优化**：大量引用时使用虚拟滚动

## 文件清单

- ✅ 后端：`rust-lib/flowy-ai/src/middleware/chat_service_mw.rs`
- ✅ 前端组件：`appflowy_flutter/lib/plugins/ai_chat/widgets/unified_reference_display.dart`
- ✅ 翻译文件：`resources/translations/zh-CN.json`, `en-US.json`
- ⏳ 集成文件：`appflowy_flutter/lib/plugins/ai_chat/presentation/message/ai_text_message.dart`
- ✅ locale keys：`appflowy_flutter/lib/generated/locale_keys.g.dart`

## 总结

通过创建统一的引用显示组件，我们实现了：
1. **一致的用户体验** - 所有引用使用相同的视觉风格
2. **更好的可维护性** - 单一组件管理所有引用类型
3. **更容易扩展** - 添加新引用类型只需添加一个enum值

这个修改与用户截图中网络搜索引用的样式完全一致，提供了专业统一的UI体验！

